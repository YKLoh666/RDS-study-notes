# 2023 June Exam

## Question 4

a)

```python
orders_rdd = raw_orders_rdd.map(lambda x: (x[1], int(x[2])))
```

b)

```python
orders_rdd.count()
```

c)

```python
orders_rdd.map(lambda x: (x[0], 1)).reduceByKey(lambda x, y: x + y)
```

d)

```python
orders_rdd.sortBy(lambda x: x[0])
```

e)

```python
orders_rdd.sortBy(lambda x: -x[1])
```

f)

```python
orders_rdd.filter(lambda x: x[1] > 50)
```

g)

```python
orders_rdd.map(lambda x: x[1]).sum() / orders_rdd.count()
```

h)

```python
orders_rdd.reduceByKey(lambda x, y: x + y)
```
